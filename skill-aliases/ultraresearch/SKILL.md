---
name: ultraresearch
description: Shorthand alias for ulw-research. Triggers the full ulw-research maximum-saturation research loop.
metadata:
  short-description: "/ultraresearch shorthand — runs ulw-research"
---

# /ultraresearch — Shorthand for ulw-research

This is a thin alias for the full `ulw-research` skill.

## Instructions

1. Read the `ulw-research` skill by opening `../ulw-research/SKILL.md` with `view_file`. Follow all instructions there exactly.
2. Execute the full `ulw-research` procedure. Do NOT stop at the alias — run the entire workflow.
