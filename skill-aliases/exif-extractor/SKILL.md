---
name: exif-extractor
description: "Extract metadata, calculate hashes (MD5, SHA-256), and verify file magic signatures to detect extension spoofing. Triggers: exif, metadata, hashes, spoofing, signature check."
metadata:
  short-description: "Extract file metadata, hashes, and check for file spoofing"
---
# exif-extractor
Run: node scripts/extract-metadata.mjs <file_path>
