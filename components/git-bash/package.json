{
	"name": "@lazyantigravity/git-bash",
	"version": "0.1.0",
	"description": "Antigravity hook that reminds Windows sessions to prefer the git_bash MCP.",
	"type": "module",
	"private": true,
	"bin": {
		"lazyantigravity-git-bash-hook": "./dist/cli.js",
		"omo-git-bash-hook": "./dist/cli.js"
	},
	"files": ["dist", "hooks"],
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"test": "bun test test/*.test.ts",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "^25.7.0",
		"typescript": "^6.0.3"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
