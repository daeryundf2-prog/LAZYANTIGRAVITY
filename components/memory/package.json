{
	"name": "@lazyantigravity/memory",
	"version": "0.1.0",
	"description": "Local Active Memory and Facts persistence runtime for Antigravity sessions.",
	"type": "module",
	"license": "MIT",
	"bin": {
		"lazyantigravity-memory": "./dist/cli.js",
		"omo-memory": "./dist/cli.js"
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"test": "node --test test/*.test.mjs",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "^25.7.0",
		"typescript": "^6.0.3"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
